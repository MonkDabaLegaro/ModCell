# Optional absolute path to adb. Defaults to `adb` from PATH.
MODCELL_ADB_PATH=

# Daemon binds to loopback by default. Keep this local unless you intentionally secure remote access.
MODCELL_HOST=127.0.0.1
MODCELL_PORT=4317
